# Linux Controller Mapper

### Development

- To run in debug mode, run `cargo run`
- To create a build with an AppImage, run `cargo appimage`
  - If it's your first time doing a build, you'll need to run `cargo install cargo-appimage` first.
